<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appsophos">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSophos">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key"></meta-data>
        <activity
            android:name=".activities.MapActivity"
            android:exported="false"
            android:theme="@style/Theme.AppSophos.NoActionBar"/>
        <activity
            android:name=".activities.DocViewActivity"
            android:exported="false"
            android:label="@string/title_activity_doc_view"
            android:theme="@style/Theme.AppSophos.NoActionBar" />
        <activity
            android:name=".activities.DocsActivity"
            android:exported="false"
            android:label="@string/title_activity_docs"
            android:theme="@style/Theme.AppSophos.NoActionBar" />
        <activity
            android:name=".activities.MenuActivity"
            android:exported="false"
            android:label="@string/title_activity_menu"
            android:theme="@style/Theme.AppSophos.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>